# コミット e4f5325f の理解

`chore: 中間生成物の実装計画を削除` — 1ファイル削除、78行減、それだけのコミットです。

## 何をしたか

`docs/superpowers/plans/2026-08-16-development-process-artifacts.md` を削除しました。これは
`lilpacy/concepts/ソフトウェア開発プロセス.md` に「工程別の成果物マップ」を追加する作業のために
書かれた実装計画で、作業が完了したので消した、という後片付けのコミットです。

## なぜ消せたのか

この計画ファイルは、同じブランチの3コミットで**作られ、更新され、そして役目を終えました**。

| コミット | 内容 | 計画ファイル |
|---|---|---|
| `2a53eb61` | 工程と成果物の対応表・精緻化系列を Concept に追加 | 作成（+80行） |
| `6818a609` | 概念設計・ドメイン設計を全体フロー図へ統合 | 更新 |
| `5ecee513` | 分岐と成果物表を「条件付き活動」として整合 | 更新 |
| `e4f5325f` | — | **削除** |

計画に書かれていた成果物（工程別成果物表、Mermaid の精緻化系列、概念設計・ドメイン設計の起動条件）は
すでに正本側に入っています。実際、現在の `lilpacy/concepts/ソフトウェア開発プロセス.md:70` に
`## 工程と成果物の対応` セクションと `工程 / この工程で確定・更新すること / 代表成果物 / 次工程へ渡す状態`
の表が存在します。つまり計画の内容は Concept へ**移送済み**で、計画ファイル自体は同じ情報の
古いコピーになっていた、というのが削除の理由です。コミットメッセージの「中間生成物」がまさにこれを指しています。

## 情報は失われていない

計画を消しても記録は残ります。`lilpacy/log.md` に3件の `[2026-08-16] synthesis` エントリがあり、
そこには計画ファイルの作成・更新の事実と、各段階での**判断の理由**（例:
「起動条件を後段の表だけに置くと全体図から設計活動が欠落するため、条件分岐とフィードバックを
全体フロー自体へ統合した」）が書かれています。vault の log が時系列の記録層を担っているので、
計画ファイルは残す必要がありません。加えて git 履歴からいつでも `git show e4f5325f` で復元できます。

なお `docs/superpowers/plans/` 自体は残っており、`2026-07-21-query-ingest.md` が今もあります。
ディレクトリの廃止ではなく、この1件の後片付けです。

## 次の一手

- レビュー観点としては「計画に書かれていて Concept に反映されなかった項目がないか」だけが確認点です。
  特に計画末尾の検証手順（`ruby scripts/wiki_pipeline_lint.rb .`、独立 source lineage の目視確認）は
  実行された前提になっているので、気になるなら lint を再実行して確認できます。
- `docs/superpowers/plans/` に残る `2026-07-21-query-ingest.md` も完了済みなら同じ扱いにするか、
  という運用ルールは未定です。「完了した plan は消す」を明文化するかは判断の余地があります。

この規模の変更（単純な1ファイル削除）なのでクイズは省略します（必要なら言ってください）。
