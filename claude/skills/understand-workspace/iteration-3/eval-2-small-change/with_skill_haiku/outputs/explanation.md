# コミット e4f5325f の説明

## Background（なぜ）

このリポジトリは「LLM Wiki」の実装である。LLM Wikiのアーキテクチャは3層で構成されている：

- **Raw sources**: 一次情報。LLMは読むだけで変更しない。
- **The wiki**: LLMが生成・管理するMarkdownファイル群。要約（summaries）、コンセプト（concepts）、エンティティ（entities）、マップ（maps）などが含まれる。
- **The schema**: LLMへの指示書。`CLAUDE.md`と`AGENTS.md`がここに該当。

このプロジェクトでは、特に`lilpacy/` ディレクトリ内にこのwikiを構築している。

削除されたファイル `docs/superpowers/plans/2026-08-16-development-process-artifacts.md` は、wikiの中核であるコンセプト `lilpacy/concepts/ソフトウェア開発プロセス.md` を**改良するための実装計画**だった。この計画は、ソフトウェア開発の各工程で「何を確定し、どの成果物として次工程へ渡すか」を整理し、コンセプトを図示・テンプレート化する仕様を詳細に記述していた。

## Intuition（本質）

このコミットは「実装計画（中間生成物）を削除する」という**cleanup**である。

実装計画が削除された後、実際のコンセプトページ `lilpacy/concepts/ソフトウェア開発プロセス.md` を見ると、**計画の内容はすでに実装済み**である。コンセプトのfrontmatterに、計画で列挙された`inputs`（参照すべき要約ページ）が実装済みである：

```
inputs:
  - "[[summaries/概念設計が、その後のコードの命運を左右する]]"
  - "[[summaries/ドメイン駆動設計再考]]"
  - "[[summaries/継続的ドキュメンテーション]]"
```

本文には、設計の起動条件（曖昧さ、ルール化の必要性）を条件分岐として含むMermaidフローチャートがあり、工程別成果物の考え方も記述されている。

つまり：

```
計画ファイル（docs/superpowers/plans/...）
    ↓（実装）
wiki層のコンセプトページ（lilpacy/concepts/...）へ投影済み
    ↓（コミット後）
不要になった計画ファイルを削除
```

## Code（実装内容）

削除されたファイルの構成を見ると：

1. **冒頭**: 目標（ゴール）と方針を記述
   - 「工程名だけでなく『いつ、何を確定し、どの成果物として次工程へ渡すか』が分かる正本へ更新する」

2. **変更範囲**: 具体的にどのファイルを更新するかを列挙
   - `lilpacy/concepts/ソフトウェア開発プロセス.md` を更新
   - `lilpacy/index.md`の説明を合わせる
   - `lilpacy/log.md`に記録する

3. **工程別成果物の設計**: 企画から運用まで11の工程それぞれについて確定対象と代表成果物を表定義

4. **成果物の精緻化系列**: Mermaidで示す5つの系列（課題→要件→テスト、業務→仕様→実装など）

5. **概念設計・ドメイン設計の統合**: 条件付きで起動される活動の流れ

6. **検証と完了**: 5段階のチェックリスト
   - lint実行
   - 新規claimが2つ以上の根拠を持つことを確認
   - mermaidに起動条件と戻り先が含まれていることを確認
   - PRを作成

**なぜ削除したのか**: コミット直後を見ると（git log），`bbab2b4f Merge pull request #91 from lilpacy/worktree/paints-process-map`など、別の作業が進行している。この計画の実装仕様は`lilpacy/`側へ投影済みであり、計画ドキュメント自体は、wikiが確定した段階で削除するのが整理の流儀である。wikiの層分けでは、計画（schema層への指示）は実装ワーク中は保持が必要だが、投影完了後の`docs/superpowers/plans/`配下のファイルは、実装が完了したら履歴から削除する慣行を示している。

## Quiz

このコミットの意図を確認するための問題です。各問題を読み、最も適切な選択肢を選んでください。

### 問1: このファイルはなぜ削除したのか？

**選択肢**

A. ソフトウェア開発プロセスの概念そのものが不要になったから  
B. 計画に記載された内容がwiki層のコンセプトページへ実装済みであり、中間生成物としての役割が終わったから  
C. ドキュメント管理を簡潔にするため、全ての計画ファイルを一括削除したから  
D. `docs/superpowers/plans/`ディレクトリが非推奨になったから  

**正解: B**

- **Aが誤り**: コンセプトページの更新日時（updated: 2026-08-16）から、むしろこの日に内容が最新化されたばかり。
- **Bが正解**: 計画に記載された「工程別成果物表」「Mermaidフロー」「inputs列挙」などが実装ページへ投影済み。計画は指標書であり、実装後は削除が正当。
- **Cが誤り**: 同時期に作られた他の計画ファイル（例: `2026-07-21-query-ingest.md`）は残っており、全削除ではない。
- **Dが誤り**: 同じディレクトリに他の計画ファイルがまだある。

### 問2: このコミットが削除したのは「何が何を左右する」という研究成果ではなく、どの層の成果物だったのか？

**選択肢**

A. wiki層（コンセプト・サマリーなど）  
B. raw層（生ノート・一次情報）  
C. schema層（指示書・テンプレート）  
D. 計画・中間生成物（wiki実装の進捗追跡ドキュメント）  

**正解: D**

- **Aが誤り**: コンセプトページ自体（wiki層）は残っており、むしろ投影完了状態。
- **Bが誤り**: raw層は編集対象ではなく、削除も対象外。
- **Cが誤り**: `AGENTS.md`や`CLAUDE.md`などschema層は保持されている。
- **Dが正解**: 削除されたのは`docs/superpowers/plans/`配下の計画ドキュメント。これはwiki実装中の指標・TODO的性質を持ち、投影完了後は削除するのが設計。

### 問3: 削除されたファイルに記載されていた「11の工程」の最後（11番目）は何か？

**選択肢**

A. テスト  
B. リリース・移行  
C. 運用・保守・改善  
D. 詳細設計  

**正解: C**

- **Aが誤り**: 9番目。
- **Bが誤り**: 10番目。
- **Cが正解**: 削除ファイルの「工程別成果物の設計」に「11. 運用・保守・改善」と記載。
- **Dが誤り**: 7番目。

---

このクイズで全問正解できたなら、このコミットの意図（**完了した実装計画を履歴から削除する cleanup**）が理解できています。誤答があれば、その領域について質問してください。
