interface:
  display_name: "Claude Fable Review"
  short_description: "Run Fable review only when explicitly requested"
  default_prompt: "Use $claude-fable-review only if the user explicitly asked for Fable review or to consult Fable."
