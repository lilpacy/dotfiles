" ==================================================
" Vim Editor
" ==================================================

syntax on

set encoding=utf-8
set fileformats=unix,dos,mac

set tabstop=4
set shiftwidth=4

set clipboard+=unnamed

set number

" ==================================================
" Filer (Netrw)
" ==================================================

" サイズを(K, M, G)で表示
let g:netrw_sizestyle="H"

" 日付フォーマットを yyyy/mm/dd(曜日) hh:mm:ss で表示
let g:netrw_timefmt="%Y/%m/%d(%a) %H:%M:%S"

" ヘッダを非表示にする
let g:netrw_banner=0

" プレビューウィンドウを垂直分割で表示する
let g:netrw_preview=1

" 左右分割を右側に開く
let g:netrw_altv = 1

" 分割で開いたときに80%のサイズで開く
let g:netrw_winsize = 80

" 表示形式をツリービューに変更
let g:netrw_liststyle = 3

" Enterキーで新しいタブで開く
let g:netrw_browse_split = 3

" ==================================================
" Netrw 基本操作メモ
" ==================================================

" 【基本的な起動方法】
" :Ex       -> カレントバッファでNetrwを開く
" :Sex      -> 水平分割で開く
" :Vex      -> 垂直分割で開く
" :Tex      -> 新しいタブで開く

" 【移動】
" j / k     -> 上下移動
" Enter     -> ファイルを開く or ディレクトリに移動
" -         -> 一つ上のディレクトリに移動
" u         -> 前のディレクトリに戻る
" U         -> u で戻る前のディレクトリに進む

" 【ファイルの開き方】
" o         -> 水平分割で開く
" v         -> 垂直分割で開く
" t         -> 新しいタブで開く
" p         -> プレビューウィンドウで開く

" 【ファイルやディレクトリの操作】
" d         -> 新しいディレクトリを作成
" D         -> ファイル・空ディレクトリを削除
" R         -> 名前を変更

" 【マークを使った操作（複数ファイルの処理）】
" mf        -> ファイルをマーク
" mt        -> 移動/コピー先のディレクトリを指定
" mc        -> マークしたファイルをコピー
" mm        -> マークしたファイルを移動
" mu        -> すべてのマークを解除

" 【表示のカスタマイズ】
" I         -> ヘッダの表示/非表示切り替え
" i         -> 表示形式の変更（リスト・ツリーなど）
" s         -> ソート順変更（名前・時間・サイズ）
" r         -> ソート順を逆順にする
" a         -> 隠しファイルの表示/非表示切り替え

